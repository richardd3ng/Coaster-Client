import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        width: 10,
        height: 10,
        borderRadius: 30,
        position: "absolute",
        top: 4,
        right: 4,
    },
});

export default styles;
