import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        width: 20,
        height: 20,
        borderRadius: 20,
        position: "absolute",
        top: 4,
        right: 4,
    },
});

export default styles;
