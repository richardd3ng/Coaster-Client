import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    button: {
        width: 40,
        height: 40,
        borderRadius: 20,
    },
    icon: {
        width: 24,
        height: 24,
    }
});

export default styles;
